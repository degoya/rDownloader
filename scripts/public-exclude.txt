# What scripts/export-public.sh leaves out of the public repository (RD-130-23).
#
# One path per line, relative to the repository root; a trailing slash marks a directory. Blank
# lines and lines starting with `#` are ignored. Everything else in `git archive <ref>` is public.
# A path listed here that the exported tree does not have is reported, not an error: an older
# tag may predate it.

# The developer documentation, as a whole: the roadmap and the German job files, the audits,
# loose notes, design drafts, the ADRs and the crate-by-crate reference. The owner regards it as
# internal material. What a user, a contributor or a plugin author needs from it is in the wiki
# (scripts/export-wiki.sh): installation, building from source, the plugin reference. Nothing
# that stays public may need it — the tables tests compare with the code sit beside that code
# (crates/rd-core/recovery-matrix.md, crates/rd-api/mcp-coverage.md), the README's screenshots
# under .github/readme/ — and export-public.sh refuses a public file that links into it.
docs/

# The working card for coding agents and their configuration.
AGENTS.md
CLAUDE.md
.claude/

# An early specification.
specs.md

# A test that only makes sense next to the planning files. scripts/measure-mega-login-fuel.sh
# stays public: plugins/mega-login-probe cites it.
crates/rdownloader/tests/job_status_words.rs
