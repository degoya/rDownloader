[package]
name = "rd-media"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
anyhow.workspace = true
async-trait.workspace = true
chrono.workspace = true
quick-xml.workspace = true
rd-core = { path = "../rd-core" }
rd-db = { path = "../rd-db" }
rd-files = { path = "../rd-files" }
# The one cookie scope rule, `CookieScope::admit` (RD-120-52). Already in the graph through
# rd-scheduler, so this adds no crate and no cycle.
rd-http = { path = "../rd-http" }
rd-limits = { path = "../rd-limits" }
rd-scheduler = { path = "../rd-scheduler" }
rd-tools = { path = "../rd-tools" }
rd-secrets = { path = "../rd-secrets" }
secrecy.workspace = true
serde.workspace = true
serde_json.workspace = true
tempfile.workspace = true
tokio.workspace = true
tokio-util.workspace = true
tracing.workspace = true
url.workspace = true

[lints]
workspace = true

[dev-dependencies]
tempfile.workspace = true
